#include "../clarify.h"

RUN_TESTS()
{
    GIVEN("given")
    {
        WHEN("when")
        {
            /* THEN */
            THEN("then")
            {
                REQUIRE(false);
            }
        }
    }
}